<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <style>
        /* Reset some default styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Set background and font settings */
        body {
            font-family: Arial, sans-serif;
            background-color: #106EBE;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            position: relative;
        }

        /* Container for responsive layout */
        .container {
            text-align: center;
            background: #0FFCBE;
            padding: 20px;
            box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            max-width: 400px;
            width: 100%;
        }

        /* Heading styling */
        h2 {
            font-size: 1.5em;
            color: #333;
            margin-bottom: 20px;
        }

        /* Button Styling */
        a {
            text-decoration: none;
        }

        button {
            padding: 12px 24px;
            font-size: 1rem;
            color: white;
            background-color: #007bff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            width: 100%;
        }

        button:hover {
            background-color: #0056b3;
        }

        /* Footer Styling */
        footer {
            position: absolute;
            bottom: 0;
            width: 100%;
            text-align: center;
            background-color: #0FFCBE;
            padding: 10px;
            font-size: 1rem;
            border-top: 1px solid #ccc;
        }

        footer h3 {
            margin: 0;
            color: #333;
        }

        /* Mobile responsiveness */
        @media only screen and (max-width: 600px) {
            h2 {
                font-size: 1.25em;
            }

            button {
                font-size: 0.9rem;
                padding: 10px 20px;
            }

            footer {
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Total Number of Users are : <%= count %></h2>
        <a href="http://localhost:8080/user/new">
            <button>Join us Today</button>
        </a>
    </div>
    <footer>
        <h3>made by: Ritik Shankar</h3>
    </footer>
</body>
</html>
